# Workspace and UX pilot release — 13 September 2026

This records the earlier UX pilot. The subsequent company-receipt and Director identity release is documented in [Company receiving accounts](COMPANY-RECEIVING-ACCOUNTS.md#production-release--13-september-2026).

## Deployed version

- Production: https://benoxfordhub.online/ on the existing Hostinger account.
- Source: `819a817c5f238d9455942729ba0fc8dbaa43fc0c`, pushed to `main` at https://github.com/M3-AI-alt/BOH_APP.
- Hostinger build: `01a0997d-36db-703e-a204-48970a525e52`, completed at **06:39:39 UTC / 13:39:39 Vietnam time**.
- Deployment source: sanitized committed archive `boh-release-819a817c5f23.tar.gz`, built with Node 24 and `build:hostinger`, serving `dist/standalone/hostinger-server.mjs`.
- Existing domain, hosting, Supabase project, authentication configuration and staff roles were retained. No Vercel deployment, paid activation or official accounting cutover.
- This documentation-only follow-up records evidence; the application version above is unchanged.

## Database and recovery checks

Applied only these two additive migrations, before the application deployment; no seeds or role initialization:

1. `20260912174225_workspace_entry_safety.sql`
2. `20260912185432_ux_saved_views.sql`

All 11 repository migrations now match production migration history. Before migration, private application (`public`, `boh_private`) and Auth schema/data backups were saved outside Git and the deployment archive. Restoration into an isolated QA database recovered **6 Auth accounts, 6 staff accounts and 10,579 application/source records**. Both migrations and `workspace.sql` / `ux_saved_views.sql` tests then passed against that fresh restored copy. Synthetic test writes were rolled back and were never run against production.

Before/after production audit: **205 students, 7,001 attendance marks, 107 receipts, 129 expenses and 128 packages**, unchanged. The audited business-record fingerprint was identical before and after migration. August collections remain **VND 200,295,000**; January–May expenses remain included. No financial documents, journals, settlements or staged imports were created by this deployment.

Stored document-file restoration is not verified by the database-only restore drill. Never restore this backup over live staff work without a separately reviewed recovery decision.

## Verification

- 107 automated application tests passed; TypeScript, both production build targets and production dependency audit passed (0 reported vulnerabilities).
- Production grants deny anonymous and browser-authenticated direct access to the new draft/view tables and RPCs; the service role retains server access. Read-only RPC checks confirmed attendance views are available to all six active staff identities and receipt views are denied to TAs. Private draft lists contain no production entries.
- Live signed-out `/api/drafts`, `/api/views` (attendance and receipts), `/api/state`, `/api/accounting` and `/api/auth/status` return JSON **401** with no records and `Cache-Control: private, no-store`. Invalid cookies and forged legacy identity headers did not authorize access.
- Live English/Vietnamese login controls render; logo and entrance photograph load. Vietnamese login at 390px width and 200% root text enlargement has no page overflow. Browser error collection was empty.
- One normal login attempt per role using the privately saved initial credentials returned 401. No credential guessing, password resets or staff-account changes followed. **Signed-in live role checks and real staff workflow acceptance remain outstanding.** Database and synthetic-route checks are not substitutes for that acceptance.

## Existing caveats and follow-up

- Production Supabase security advisors report the existing warning that leaked-password protection is disabled; no new error was reported. Auth policy or paid features were not changed automatically.
- Although BOH sets `Vary: Cookie`, this value was not visible at the live Hostinger edge. `private, no-store` remains present and no cache-hit evidence was observed. Hostinger/HCDN normalization is the likely explanation, not verified by a direct origin comparison. Do not record `Vary: Cookie` as a passed live check.
- Private credentials, source spreadsheets, backups, generated artifacts and the unrelated local `UI-DESIGN-SKILL.md` were not pushed or deployed.
- Scope and outstanding functionality remain in [UX-first implementation](UX-FIRST-IMPLEMENTATION.md) and [complete workspace status](WORKSPACE-IMPLEMENTATION.md). This is not completion of the whole centre-management/accounting plan.

For application rollback, use the previous known-good source deployment while preserving additive database tables and all subsequent staff entries. Do not drop populated drafts, views or command tables as an automatic rollback.
